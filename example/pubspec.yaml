name: fl_language_picker_example
description: Example application of language_picker Flutter package.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  fl_language_picker:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
